#include "complex_number_pluse.h"

double complex_number_pluse(double A, double BI,double C,double DI)
{
	return (BI + DI);
}
double real_number_pluse(double A, double B, double C, double D)
{
	return(A + C);
}