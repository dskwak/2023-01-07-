#include "complex_number_minuse.h"

double complex_number_minuse(double A, double BI, double C, double DI)
{
	return (BI*C)+(A*DI);
}
double real_number(double A, double B, double C, double D)
{
	return (A * C) - (B * D);
}